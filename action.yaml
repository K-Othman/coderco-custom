name: CoderCo Custom Action

description: Greet someone

inputs:
  who-to-greet:
    description: who to greet
    required: true
    default: Hello

outputs:
  time:
    description: Time greeted

runs:
  using: node24
  main: dist/index.js